server:
  port: '8090'
spring:
  datasource:
    url: 'jdbc:mysql://localhost/rvdor_tickets'
    username: root
    password: '123456'
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
      dialect: 'org.hibernate.dialect.MySQL8Dialect'
    hibernate:
      ddl-auto: 'update'